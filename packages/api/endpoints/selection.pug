div#selection
    p #{selection}

    div#form-select
        select(hx-get="/form" hx-target="#form" hx-swap="outerHTML")
            each form in availableForms
                option(value=form.id) #{form.name}

div#form