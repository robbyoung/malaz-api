mixin sceneNav(previousSceneId, nextSceneId)
    div(id="scene-nav")
        if previousSceneId
            a(
                href="/scene/" + previousSceneId
                class="button ochre" 
            ) Previous
        else
            div
        if nextSceneId
            a(
                href="/scene/" + nextSceneId
                class="button ochre" 
            ) Next
        else
            div