div#search-results
    each result in results
        button(
            type="button" 
            onclick=`document.getElementById("search-box").value = "${result}"`
        ) #{result}
    if results.length === 0
        p No matches