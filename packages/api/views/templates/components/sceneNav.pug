mixin sceneNav(previousSceneId, nextSceneId)
    div(id="scene-nav")
        if previousSceneId
            a(
                href="/scenes/" + previousSceneId
                class="button ochre" 
            ) Previous
        else
            div
        if nextSceneId
            a(
                href="/scenes/" + nextSceneId
                class="button ochre" 
            ) Next
        else
            div